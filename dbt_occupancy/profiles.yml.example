dbt_occupancy:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('DBT_HOST','localhost') }}"
      user: "{{ env_var('DBT_USER','postgres') }}"
      password: "{{ env_var('DBT_PASSWORD','postgres') }}"
      dbname: "{{ env_var('DBT_DB','occupancy') }}"
      schema: "{{ env_var('DBT_SCHEMA','public') }}"
      port: "{{ env_var('DBT_PORT','5432') | int }}"
      threads: 4
      keepalives_idle: 0
      connect_timeout: 10
      search_path: "public"
