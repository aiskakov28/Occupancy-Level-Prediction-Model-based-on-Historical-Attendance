groups:
- name: uptime
  rules:
  - alert: ServiceDown
    expr: up == 0
    for: 1m
    labels: { severity: page }
    annotations:
      summary: "Instance {{ $labels.instance }} down"
      description: "Job {{ $labels.job }} on {{ $labels.instance }} is not up."
